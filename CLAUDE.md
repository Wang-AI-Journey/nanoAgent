# nanoAgent 项目编程习惯

## 项目定位

教学用极简 Agent 仓库，每个文件控制在 100 行以内，优先可读性而非工程完整性。

## 文件命名规范

- 代码文件：`{模块}-{功能}.py`，如 `agent-essence.py`、`nano_mcp_http_server.py`
- 文档文件：`{系列}-{序号}-{主题}.md`，如 `nano-skill-01-what-is-skill.md`
- 目录按主题分层：`01-essence`、`02-memory`、`03-skills-mcp`...

## 代码风格

- 每个演示文件独立可运行，不依赖项目内其他模块
- 不封装 class，能用函数解决的不用 class
- 不加 logging、argparse 之外的依赖（除非演示对象本身就是该依赖）
- 变量名、函数名用英文，注释和 print 输出用中文

## 文档风格

- Markdown 文章面向有基础的开发者，不解释 Python 语法等基础知识
- 每篇文章配套一个可运行的代码示例
- 代码块内注释用中文，解释"为什么"而不是"是什么"

## 修改原则

- 演示代码改动越小越好，保持"最小可理解单元"
- 新增内容优先新建文件，不改现有教学示例
- 目录序号已固定，不随意调整顺序
